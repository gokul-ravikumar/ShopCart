<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>digg it!! · Reset Password</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div
      class="min-h-screen bg-gradient-to-br from-orange-50 to-orange-100 flex items-center justify-center px-4"
    >
      <div
        class="w-full max-w-md bg-white border border-gray-200 rounded-2xl shadow-2xl overflow-hidden"
      >
        <div class="p-6">
          <h1 class="text-2xl text-center font-semibold">Reset Password</h1>
          <p class="text-center text-gray-600">
            Enter your new password below
          </p>
        </div>

        <div class="px-6 pb-6">
          <% if(message && messageType) { %>
            <% if(messageType === 'error') { %>
              <div class="mb-4 p-3 text-sm text-red-700 bg-red-100 border border-red-200 rounded text-center">
                <%= message %>
              </div>
            <% } %>

            <% if(messageType === 'success') { %>
              <div class="mb-4 p-3 text-sm text-green-700 bg-green-100 border border-green-200 rounded text-center">
                <%= message %>
              </div>
            <% } %>
          <% } %>

          <!-- Reset Password Form -->
          <form method="post" action="/user/reset-password" class="space-y-4">

            <!-- Phone passed from OTP page -->
            <input type="hidden" name="phone" value="<%= phone || '' %>" />

            <div class="space-y-2">
              <label for="password" class="block text-sm font-medium">New Password</label>
              <input
                id="password"
                name="password"
                type="password"
                required
                minlength="6"
                placeholder="Enter new password"
                class="w-full border rounded px-3 py-2"
              />
            </div>

            <div class="space-y-2">
              <label for="confirmPassword" class="block text-sm font-medium">Confirm Password</label>
              <input
                id="confirmPassword"
                name="confirmPassword"
                type="password"
                required
                minlength="6"
                placeholder="Confirm new password"
                class="w-full border rounded px-3 py-2"
              />
            </div>

            <button
              type="submit"
              class="w-full px-4 py-2 rounded bg-orange-600 hover:bg-orange-700 text-white font-medium"
            >
              Update Password
            </button>
          </form>

          <div class="mt-4 text-center">
            <a href="/user/login" class="text-gray-500 text-sm hover:text-orange-600">
              Back to Sign In
            </a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
