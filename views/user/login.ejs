<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>digg it!! · Sign In</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/app.css" />
</head>

<body>
  <div class="min-h-screen bg-gradient-to-br from-orange-50 to-orange-100 flex items-center justify-center px-4">
    <div class="w-full max-w-md bg-white border border-gray-200 rounded-2xl shadow-2xl overflow-hidden">
      <div class="p-6">
        <div class="flex justify-center mb-4">
          <div class="text-4xl">🍽️</div>
        </div>
        <h1 class="text-2xl text-center font-semibold">Welcome Back</h1>
        <p class="text-center text-gray-600">Sign in to your account to continue</p>
      </div>

      <div class="px-6 pb-6">
        <% if (message) { %>
          <div class="mb-4 p-3 text-sm text-red-700 bg-red-100 border border-red-200 rounded">
            <%= message %>
          </div>
          <% } %>

            <form method="post" action="/user/login" class="space-y-4">
              <div class="space-y-2">
                <label for="email" class="block text-sm font-medium">Email</label>
                <input id="userEmail" name="user[email]" type="email" placeholder="Enter your Mail ID"
                  class="w-full border rounded px-3 py-2" required />
              </div>

              <div class="space-y-2">
                <div class="flex items-center justify-between">
                  <label for="password" class="block text-sm font-medium">Password</label>
                  <a href="/user/forgot-password" class="text-sm text-orange-600 hover:text-orange-700">Forgot
                    password?</a>
                </div>
                <input id="userPassword" name="user[password]" type="password" placeholder="••••••••"
                  class="w-full border rounded px-3 py-2" required />
              </div>

              <button type="submit" class="w-full px-4 py-2 rounded bg-orange-600 hover:bg-orange-700 text-white">
                Sign In
              </button>
            </form>

            <div class="mt-6 text-center text-sm">
              <span class="text-gray-600">Don't have an account? </span>
              <a href="/user/register" class="text-orange-600 hover:text-orange-700">Register Here</a>
            </div>
      </div>
    </div>
  </div>
</body>

</html>