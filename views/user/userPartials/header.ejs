        <!-- Header -->
        <header
          class="bg-white shadow p-4 pt-6 pb-7 flex justify-between items-center sticky top-0 z-50"
        >
          <h1 class="text-xl font-semibold"><%= pageTitle %></h1>
          <span class="text-sm text-gray-600"
            >Welcome <%= user?.name || 'User' %></span
          >
        </header>